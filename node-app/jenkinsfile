pipeline{
    agent {
        docker (image "ubuntu:latest")
    }
    stages{
        stages{
            stage("Build"){
                steps{
                    sh 'npm run install'
                    

                }

            }
            stage('Running the app'){
                steps{
                    sh 'node server.js'
                }
            }
        }
    }
}