pipeline {
    agent {
        docker { image 'node:lts-slim' }
    }
    stages {
        stage('Build') {
            steps {
                sh 'pwd && ls'
                sh 'npm install'
            }
        }
        stage('Running the app') {
            steps {
                sh 'node server.js'
            }
        }
    }
}
