pipeline{
    agent {
        docker (image "node:latest")
    }
    stages{
        stages{
            stage("Build"){
                steps{
                    sh 'npm run install'
                    

                }

            }
            stage('Running the app'){
                steps{
                    sh 'node server.js'
                }
            }
        }
    }
}